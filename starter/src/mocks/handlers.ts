// import {delay, HttpResponse, http} from 'msw' // Removed
// import fruits from './data/fruits.json' with {type: 'json'} // Removed

export const handlers = [
	// http.get('/fruits', async () => {
	// 	await delay('real')
	// 	return HttpResponse.json(fruits)
	// }) // Removed
]
